Програма мінімум:

Розширити структуру, яку побудували на уроці, можливістю вставки дерева в наявне дерево та видалення піддерева з дерева, що існує.

 

Середній рівень:

-

Програма максимум:

Написати алгоритм, що буде парсити html документ та зберігати його Document Object Model (DOM) у дереві. Дерево повинно зберігати тег та текст, обрамлений цим тегом (якщо є такий). Додати можливість пошуку тексту за тегом.

Вхідні дані: html документ та тег

Вихідні дані: текст, якщо є.